// Navigation now handled by expo-router (file-based routes in `app/`).
// Keep this file minimal to avoid registering an additional navigator.
import React from 'react';

export default function App() {
  return null;
}
